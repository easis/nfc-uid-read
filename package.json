{
    "private": true,
    "scripts": {
        "dev": "next",
        "build": "next build && next export",
        "deploy": "next build && type nul > out/.nojekyll && git add out/ && git commit -m \"Deploy\" && git subtree push --prefix out origin gh-pages"
    },
    "dependencies": {
        "next": "latest",
        "react": "latest",
        "react-dom": "latest"
    },
    "devDependencies": {
        "@types/node": "^18.7.6",
        "@types/react": "^18.0.17",
        "autoprefixer": "^10.4.8",
        "postcss": "^8.4.16",
        "tailwindcss": "^3.1.8",
        "typescript": "^4.7.4"
    }
}